program CollabMoon;

uses
  System.StartUpCopy,
  FMX.Forms,
  FMain in 'FMain.pas' {Form14};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm14, Form14);
  Application.Run;
end.
