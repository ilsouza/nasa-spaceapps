unit FPlayer;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants,
  FMX.Types, FMX.Controls, FMX.Forms, FMX.Graphics, FMX.Dialogs, FMX.Controls.Presentation, FMX.StdCtrls, FMX.Objects, ALFmxObjects, ALVideoPlayer, FMX.Layouts;

type
  TfrmPlayer = class(TForm)
    Layout1: TLayout;
    vpplayer: TALVideoPlayerSurface;
    Layout2: TLayout;
    Layout3: TLayout;
    btnBack: TButton;
    btnPlayList: TButton;
    procedure btnPlayListClick(Sender: TObject);
    procedure btnBackClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmPlayer: TfrmPlayer;

implementation

{$R *.fmx}

procedure TfrmPlayer.btnBackClick(Sender: TObject);
begin
  Close;
end;

procedure TfrmPlayer.btnPlayListClick(Sender: TObject);
begin
  ShowMessage('adicionado à playList do youtube');
end;

end.
